# 1. Data loading
def load_data(path):
    return pd.read_csv(path)

# 2. Sentence splitting
def split_sentences(text):
    ...

# 3. Tokenization wrapper (Sudachi lives here)
def tokenize_text(tokenizer, text):
    ...
